import Layout from "@/components/Layout";

const Register = () => {
    return (
        <Layout pagina="register">
            <div> REGISTER </div>
        </Layout>
    );
}

export default Register;